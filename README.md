# React + Vite

This is simple simple notes app using React and Tailwind CSS. This app covers controlled and uncontrolled components, use of forms, basic validation, form submission, form UI states, prop drilling and lifting up state.

## install dependencies and run
npm i
npm run dev
